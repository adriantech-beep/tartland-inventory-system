import MaterialSettings from "@/material-settings/MaterialSettings-v2";

const MaterialSettingsPage = () => {
  return (
    <div className=" w-full h-full flex flex-col gap-4">
      <MaterialSettings />
    </div>
  );
};

export default MaterialSettingsPage;
